hey !!!!

its too excited to see again 

now we will see how i learned link javascript to html 

first we 
\we write last of body <script src="file.js"></script>

then how we add html element to script 

example for a counter site 
we take a h2 have num 0 
and a button named as increment 

but we need to keep unique id for it 
so i kept h2 id as countel and button as increment and for function i used 
onclick=increment() # which means when i click button it will run function name increment 


wt the function actually do now ?

for click of button it will change value of 0 to increment which is header so already we linked function in button but changes should occur in h2 file so we need to add in js 

for that we use document keyword and store inn variable like 

let countel = document.getElementById("countel")

//# in explain as let countel // is an element document //keyword //getElementByid meas search that element via id // ("countel) need to writ id of element 





Next we will look the DOC (Document Object Model ) # means how you use Javascript to modify a website 

#Document 
ofcourse it about the html document  
#object 
It about object  the inside the Document 
#Model 
it we create a model of real element of the html file 




<!-- String vs Number  -->

string always wins 

console.log(a+b) #70
console.log(a+c)#2525
console.log(b+c)#4525
console.log(c+d)#2545

<!-- escape  character   \ -->
it tell the next character as string strter or peace of code 

for increment w use count = count + 1 so we can also use count += 1 



<!-- REcap -->
<!-- 
Script tag 
variables 
numbers 
strings 
console.log("lets Do recap ") 


Fuctions # onclick="function()"


-->

